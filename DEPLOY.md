# German reading experiment

Run locally with Vite on port 5176 and the API on port 3003:

```bash
npm run dev
```

No Rosti app exists for German yet. If deployment is added later, set
`ROSTI_APP_ID` explicitly; this repository does not provide an app id.
